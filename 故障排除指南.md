# 🔧 SmartFit 故障排除指南

## ❌ 问题：端口被占用 + 锁文件冲突

### 错误信息
```
⚠ Port 3000 is in use by process 25656, using available port 3001 instead.
⨯ Unable to acquire lock at .next\dev\lock, is another instance of next dev running?
```

### 原因分析
1. **端口被占用**：另一个 Node.js 进程正在使用 3000 端口
2. **锁文件冲突**：之前的 Next.js 进程没有正确关闭，留下了锁文件

---

## ✅ 解决方案

### 方法 1：终止占用端口的进程（推荐）

**步骤：**

1. **查找占用 3000 端口的进程**
   ```powershell
   netstat -ano | findstr :3000
   ```
   或者
   ```powershell
   Get-NetTCPConnection -LocalPort 3000 | Select-Object OwningProcess
   ```

2. **终止进程**（替换 PID 为实际进程 ID）
   ```powershell
   Stop-Process -Id <PID> -Force
   ```
   例如：`Stop-Process -Id 25656 -Force`

3. **删除锁文件**
   ```powershell
   cd C:\Users\34023\Desktop\yzh\smart-fit
   Remove-Item .next\dev\lock -Force -ErrorAction SilentlyContinue
   ```

4. **重新启动**
   ```powershell
   npm run dev
   ```

---

### 方法 2：使用其他端口

如果不想终止其他进程，可以使用其他端口：

```powershell
npm run dev -- -p 3002
```

然后访问：http://localhost:3002

---

### 方法 3：一键清理脚本

创建一个 PowerShell 脚本 `cleanup.ps1`：

```powershell
# 终止所有 Next.js 相关进程
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force

# 删除锁文件
cd C:\Users\34023\Desktop\yzh\smart-fit
Remove-Item .next\dev\lock -Force -ErrorAction SilentlyContinue

# 清理 .next 文件夹（可选，会重新构建）
# Remove-Item .next -Recurse -Force -ErrorAction SilentlyContinue

Write-Host "✅ 清理完成，现在可以运行 npm run dev"
```

运行脚本：
```powershell
.\cleanup.ps1
```

---

## 🛠️ 其他常见问题

### 问题 1：多个 Node.js 进程运行

**查看所有 Node.js 进程：**
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*node*"}
```

**终止所有 Node.js 进程（谨慎使用）：**
```powershell
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
```

---

### 问题 2：.next 文件夹损坏

**清理并重新构建：**
```powershell
cd C:\Users\34023\Desktop\yzh\smart-fit
Remove-Item .next -Recurse -Force
npm run dev
```

---

### 问题 3：依赖问题

**重新安装依赖：**
```powershell
cd C:\Users\34023\Desktop\yzh\smart-fit
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json -Force
npm install
npm run dev
```

---

## 📝 预防措施

1. **正确关闭服务器**
   - 在终端按 `Ctrl + C` 停止服务器
   - 不要直接关闭终端窗口

2. **使用任务管理器**
   - 如果服务器卡死，可以在任务管理器中终止 Node.js 进程

3. **定期清理**
   - 定期清理 `.next` 文件夹
   - 清理无用的 Node.js 进程

---

## 🎯 快速命令参考

| 操作 | 命令 |
|------|------|
| 查看端口占用 | `netstat -ano \| findstr :3000` |
| 终止进程 | `Stop-Process -Id <PID> -Force` |
| 删除锁文件 | `Remove-Item .next\dev\lock -Force` |
| 清理 .next | `Remove-Item .next -Recurse -Force` |
| 重新启动 | `npm run dev` |

---

## 💡 提示

- 如果经常遇到端口占用问题，考虑使用环境变量设置默认端口
- 可以在 `package.json` 中添加脚本：
  ```json
  "scripts": {
    "dev": "next dev -p 3001"
  }
  ```

---

**问题已解决！现在可以正常启动项目了。** ✅

